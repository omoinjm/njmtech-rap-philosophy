# API worker URL for local dev (wrangler dev in /worker on port 8787)
INTERNAL_API_URL=http://127.0.0.1:8787

# Google Sign-In (optional — same client ID as worker GOOGLE_CLIENT_ID)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
